# Core
langgraph>=0.5.4
langchain>=0.3.28
copilotkit>=0.1.39
fastapi>=0.115.14
uvicorn>=0.41.0
pydantic>=2.12.0
python-dotenv>=1.2.0

# LLM Providers (install the one you need)
langchain-anthropic>=0.3.22    # LLM_PROVIDER=anthropic (default)
langchain-openai>=0.3.0        # LLM_PROVIDER=openai
# langchain-google-genai>=2.1.0  # LLM_PROVIDER=google
# langchain-ollama>=0.3.0        # LLM_PROVIDER=ollama (free, local)
